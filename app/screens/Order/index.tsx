import React from 'react';
import { Text, View } from 'react-native';

const Order: React.FC = () => {
    return (
        <View>
            <Text>Order</Text>
        </View>
    );
};

export default Order;
